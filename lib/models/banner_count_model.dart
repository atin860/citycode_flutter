


/*
import 'dart:convert';

class Count {
  Future <bannercount> apiCount(Map<String, dynamic>param) async {
    var data = jsonDecode(response.body);
    return bannercount(status: data["status"],
        error: data["error"],
        msg: Message.fromjson(data["messages"]

        )

    );
  }
}


class bannercount{
int? status;
String? error;
Message? msg;

  bannercount(
{this.status,this.error,this.msg});

}
class Message{
String? message;
Message({required this.message});

factory Message.fromjson(dynamic json){
return Message(
message: json["success"],
);
}
}



*/
