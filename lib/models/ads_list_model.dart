class AdsListModel {
  String addId, addName, addImage, url, status, createdDate, updatedDate;

  AdsListModel(this.addId, this.addName, this.addImage, this.url, this.status,
      this.createdDate, this.updatedDate);
}